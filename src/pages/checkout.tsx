import React from "react";
import { Grid, Box, Button, Heading, Text, Main, Layer } from "grommet";
import { Close } from "grommet-icons";
import { render } from "@testing-library/react";

const Checkout = () => {
  return (
    <Main>
      <Grid>blabla</Grid>
    </Main>
  );
};

export default Checkout;
